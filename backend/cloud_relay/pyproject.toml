[project]
name = "mission-dashboard-cloud-relay"
version = "0.1.0"
requires-python = ">=3.11,<3.14"
dependencies = [
  "fastapi[standard]==0.116.1",
  "httpx==0.28.1",
]

[tool.fastapi]
entrypoint = "main:app"
