# Neon pooled PostgreSQL connection string. Keep sslmode=require.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require

# Long random secret shared only with the designated GMAT Worker computer.
MISSION_DASHBOARD_WORKER_TOKEN=replace-with-at-least-32-random-bytes
