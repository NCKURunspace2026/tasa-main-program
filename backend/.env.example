# Local Electron nodes use their Application Support data directory automatically.
# FastAPI Cloud must run only as the reconstructible synchronization relay.
MISSION_DASHBOARD_NODE_ROLE=relay

# Local nodes synchronize in the background; the minimum accepted value is 15 seconds.
MISSION_DASHBOARD_SYNC_INTERVAL_SEC=60
